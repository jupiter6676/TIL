-- 1.
SELECT ANIMAL_ID FROM ANIMAL_INS
WHERE NAME IS NULL;

-- 2.
SELECT ANIMAL_ID FROM ANIMAL_INS
WHERE NAME IS NOT NULL
ORDER BY ANIMAL_ID;

-- 3.
SELECT
  ANIMAL_TYPE,
  CASE
    WHEN NAME IS NULL THEN 'No name'
    ELSE NAME
    END AS NAME,
  SEX_UPON_INTAKE
FROM ANIMAL_INS
ORDER BY ANIMAL_ID;

-- 4.
